[project]
name = "the-loom"
version = "0.1.0"
description = "The Loom -- a living story the whole room writes at once"
requires-python = ">=3.11"
dependencies = [
    "certifi",
    "pillow",
]

[tool.uv]
package = false
